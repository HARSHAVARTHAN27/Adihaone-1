# 🏗️ Architecture Overview

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         WEB BROWSER (Frontend)                          │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                                                                 │   │
│  │  ┌─────────────────────────────────────────────────────────┐   │   │
│  │  │              Chat Interface (index.html)              │   │   │
│  │  │  ┌──────────────┐  ┌──────────────┐  ┌────────────┐  │   │   │
│  │  │  │   Message    │  │  Text Input  │  │   Buttons  │  │   │   │
│  │  │  │   Display    │  │    Field    │  │ (Send, 🎤) │  │   │   │
│  │  │  └──────────────┘  └──────────────┘  └────────────┘  │   │   │
│  │  │                                                       │   │   │
│  │  │  ┌──────────────────────────────────────────────┐    │   │   │
│  │  │  │        Reminders Panel + Settings           │    │   │   │
│  │  │  └──────────────────────────────────────────────┘    │   │   │
│  │  └─────────────────────────────────────────────────────┘   │   │
│  │                                                             │   │
│  │  Frontend Logic (script.js)                               │   │
│  │  ├─ Web Speech API (voice input/output)                   │   │
│  │  ├─ Fetch API (HTTP requests)                            │   │
│  │  ├─ Event handlers (click, keypress, input)              │   │
│  │  └─ State management (messages, reminders, settings)     │   │
│  │                                                             │   │
│  │  Styling (styles.css)                                     │   │
│  │  ├─ Responsive design                                     │   │
│  │  ├─ Animations & transitions                             │   │
│  │  └─ Modern color scheme                                  │   │
│  │                                                             │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                            ▲                                        │
│                            │ HTTP Requests                          │
│                            │ (GET/POST/DELETE)                      │
│                            ▼                                        │
└─────────────────────────────────────────────────────────────────────────┘
                              │
                              │ REST API (JSON)
                              │
┌─────────────────────────────────────────────────────────────────────────┐
│                       BACKEND (Python Flask)                            │
│                      (localhost:5000)                                   │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                   Flask Application (app.py)                   │   │
│  │                                                                │   │
│  │  ┌──────────────────────────────────────────────────────────┐ │   │
│  │  │              REST API Endpoints                         │ │   │
│  │  │                                                          │ │   │
│  │  │  POST /api/process_text  ──────┐                        │ │   │
│  │  │  POST /api/process_speech ────┐│                        │ │   │
│  │  │  POST /api/tts             ────┤├──→ Command Pipeline   │ │   │
│  │  │  GET  /api/reminders      ────┐││                       │ │   │
│  │  │  POST /api/reminders          ┼┴─→ Response            │ │   │
│  │  │  GET  /api/history            ├──→ JSON Response       │ │   │
│  │  │                                 │                        │ │   │
│  │  └──────────────────────────────────────────────────────────┘ │   │
│  │                                                                │   │
│  │  CORS Enabled ✓                                               │   │
│  │                                                                │   │
│  └──────┬─────────────────────────────────────────────────────────┘   │
│         │                                                               │
│         │ Routes to Processors                                         │
│         │                                                               │
│  ┌──────▼─────────────────────────────────────────────────────────┐   │
│  │          Processing Pipeline                                  │   │
│  │                                                                │   │
│  │  ┌────────────────────────────────────────────────────────┐   │   │
│  │  │  1️⃣ Input Reception                                   │   │   │
│  │  │     ├─ Text input from API                            │   │   │
│  │  │     └─ Speech recognition (if voice)                  │   │   │
│  │  └─────┬──────────────────────────────────────────────────┘   │   │
│  │        │                                                       │   │
│  │  ┌─────▼──────────────────────────────────────────────────────┐   │
│  │  │  2️⃣ NLP Processing (nlp_processor.py)                    │   │
│  │  │     ├─ Intent Recognition (keywords matching)           │   │
│  │  │     ├─ Entity Extraction (numbers, dates, times)        │   │
│  │  │     └─ Confidence Scoring                              │   │
│  │  │                                                          │   │
│  │  │  Recognized Intents:                                   │   │
│  │  │  ├─ time       (e.g., "What time is it?")            │   │
│  │  │  ├─ date       (e.g., "What's today?")               │   │
│  │  │  ├─ reminder   (e.g., "Remind me to...")             │   │
│  │  │  ├─ math       (e.g., "Calculate 2+2")               │   │
│  │  │  ├─ search     (e.g., "Search for...")               │   │
│  │  │  ├─ greeting   (e.g., "Hello!")                      │   │
│  │  │  ├─ help       (e.g., "What can you do?")            │   │
│  │  │  └─ goodbye    (e.g., "Goodbye!")                    │   │
│  │  └─────┬──────────────────────────────────────────────────┘   │   │
│  │        │                                                       │   │
│  │  ┌─────▼──────────────────────────────────────────────────────┐   │
│  │  │  3️⃣ Command Handling (handlers.py)                       │   │
│  │  │                                                          │   │
│  │  │  Intent Dispatcher:                                    │   │
│  │  │  ├─ handle_time()      → Current time                  │   │
│  │  │  ├─ handle_date()      → Current date                  │   │
│  │  │  ├─ handle_reminder()  → Manage reminders              │   │
│  │  │  ├─ handle_math()      → Calculate/WolframAlpha       │   │
│  │  │  ├─ handle_search()    → Web search handler            │   │
│  │  │  ├─ handle_greeting()  → Greeting response             │   │
│  │  │  ├─ handle_help()      → Help information              │   │
│  │  │  └─ handle_goodbye()   → Goodbye message               │   │
│  │  │                                                          │   │
│  │  └─────┬──────────────────────────────────────────────────┘   │   │
│  │        │                                                       │   │
│  │  ┌─────▼──────────────────────────────────────────────────────┐   │
│  │  │  4️⃣ Response Generation                                  │   │
│  │  │     ├─ Natural language response text                   │   │
│  │  │     ├─ Associated data (if applicable)                  │   │
│  │  │     └─ Intent confidence score                          │   │
│  │  └─────┬──────────────────────────────────────────────────┘   │   │
│  │        │                                                       │   │
│  │  ┌─────▼──────────────────────────────────────────────────────┐   │
│  │  │  5️⃣ Output Handling                                      │   │
│  │  │     ├─ JSON response to API                             │   │
│  │  │     ├─ Text-to-Speech (if enabled)                      │   │
│  │  │     └─ History logging                                  │   │
│  │  └──────────────────────────────────────────────────────────┘   │   │
│  │                                                                │   │
│  └────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  Supporting Modules:                                                  │
│  ├─ text_to_speech.py      (pyttsx3 wrapper)                         │
│  ├─ speech_recognition_module.py (Google Speech API)                 │
│  └─ handlers.py            (command processors)                      │
│                                                                         │
│  Data Storage:                                                         │
│  └─ In-memory (reminders, conversation history)                      │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
                              ▲
                              │ HTTP Response (JSON)
                              │
                              ▼
                         Browser Receives
                         and Displays Response
```

## Data Flow Diagram

```
User Input (Voice/Text)
        │
        ▼
[Web Speech API / Text Input]
        │
        ▼
[Fetch API → POST to Backend]
        │
        ▼
[Backend API Endpoint]
        │
        ▼
[NLP Processor: Intent Recognition]
        │
        ├─ Recognize Intent (keyword match)
        ├─ Extract Entities (numbers, dates, times)
        └─ Score Confidence
        │
        ▼
[Command Handler: Process Intent]
        │
        ├─ Execute Handler Function
        ├─ Generate Response Text
        └─ Fetch/Compute Data
        │
        ▼
[Response Generator]
        │
        ├─ Format JSON response
        ├─ Include intent & confidence
        └─ Add relevant data
        │
        ▼
[Backend Response]
        │
        ├─ HTTP 200 with JSON
        └─ (or HTTP 4xx/5xx on error)
        │
        ▼
[Frontend: Receive Response]
        │
        ├─ Display message in chat
        ├─ Update reminders (if applicable)
        └─ Text-to-Speech (if enabled)
        │
        ▼
User Sees/Hears Response ✓
```

## Component Interaction Diagram

```
┌──────────────────┐
│   Frontend UI    │
│   (HTML/CSS)     │
└────────┬─────────┘
         │
         ├──────────────────────┐
         │                      │
    ┌────▼────┐           ┌────▼──────┐
    │ Script  │           │  Styling  │
    │   .js   │           │   .css    │
    └────┬────┘           └───────────┘
         │
         ├─ Event Listeners
         ├─ API Calls (Fetch)
         ├─ Web Speech API
         └─ State Management
         │
         │
    ┌────▼─────────────┐
    │  REST API Layer  │
    │   (Flask API)    │
    └────┬─────────────┘
         │
         ├────────────────┬──────────────┬─────────────┐
         │                │              │             │
    ┌────▼────┐    ┌─────▼───┐  ┌──────▼──┐  ┌────────▼─┐
    │   NLP   │    │ Handlers│  │   TTS   │  │   STT    │
    │Processor│    │ Module  │  │ Engine  │  │ Engine   │
    └─────────┘    └─────────┘  └─────────┘  └──────────┘
         │
         └─── Intent Recognition
              Entity Extraction
              Confidence Scoring
```

## Technology Stack Visualization

```
FRONTEND TIER
┌─────────────────────────────────────────┐
│           Web Browser                   │
│  ┌────────────────────────────────────┐ │
│  │  HTML5 (Semantic Markup)           │ │
│  │  CSS3 (Responsive Design)          │ │
│  │  JavaScript (Vanilla - No Deps)    │ │
│  │                                    │ │
│  │  APIs:                             │ │
│  │  • Web Speech API                  │ │
│  │  • Fetch API                       │ │
│  │  • DOM API                         │ │
│  └────────────────────────────────────┘ │
└─────────────────────────────────────────┘

        ▲
        │ HTTP/REST
        │ JSON

┌─────────────────────────────────────────┐
│      BACKEND TIER (Python)              │
│                                         │
│  ┌────────────────────────────────────┐ │
│  │  Flask Framework (REST API)        │ │
│  │  • CORS Support                    │ │
│  │  • JSON Serialization              │ │
│  │  • Error Handling                  │ │
│  └────────────────────────────────────┘ │
│                                         │
│  ┌────────────────────────────────────┐ │
│  │  NLP Module (Intent Recognition)   │ │
│  │  • Keyword Matching                │ │
│  │  • Entity Extraction               │ │
│  │  • Pattern Matching                │ │
│  └────────────────────────────────────┘ │
│                                         │
│  ┌────────────────────────────────────┐ │
│  │  Command Handlers                  │ │
│  │  • Time Handler                    │ │
│  │  • Date Handler                    │ │
│  │  • Reminder Handler                │ │
│  │  • Math Handler                    │ │
│  │  • Search Handler                  │ │
│  └────────────────────────────────────┘ │
│                                         │
│  ┌────────────────────────────────────┐ │
│  │  Voice I/O                         │ │
│  │  • pyttsx3 (Text-to-Speech)        │ │
│  │  • SpeechRecognition (STT)         │ │
│  └────────────────────────────────────┘ │
│                                         │
│  ┌────────────────────────────────────┐ │
│  │  External APIs (Optional)          │ │
│  │  • WolframAlpha (Math & Knowledge) │ │
│  │  • Google Custom Search            │ │
│  └────────────────────────────────────┘ │
│                                         │
│  ┌────────────────────────────────────┐ │
│  │  Data Layer (In-Memory)            │ │
│  │  • Reminders Storage               │ │
│  │  • Conversation History            │ │
│  └────────────────────────────────────┘ │
│                                         │
└─────────────────────────────────────────┘

SERVICE TIER
┌─────────────────────────────────────────┐
│  External Services                      │
│  • WolframAlpha API                     │
│  • Google Speech Recognition            │
│  • Web Search APIs (optional)           │
└─────────────────────────────────────────┘
```

## Request/Response Cycle

```
1. USER ACTION
   └─ Types text or says command

2. FRONTEND PROCESSING
   ├─ Text Input:
   │  └─ User types and clicks "Send"
   └─ Voice Input:
      └─ Web Speech API captures audio

3. API REQUEST
   ├─ Method: POST
   ├─ Endpoint: /api/process_text or /api/process_speech
   └─ Body: { "text": "user input", "speak": true }

4. BACKEND PROCESSING
   ├─ Receive request
   ├─ Parse input
   ├─ Run through NLP Processor
   ├─ Match intent
   ├─ Run handler
   └─ Generate response

5. API RESPONSE
   ├─ Status: 200 OK
   └─ Body:
      {
        "response": "Here's the answer...",
        "intent": "time",
        "confidence": 0.9,
        "data": "06:47 PM"
      }

6. FRONTEND DISPLAY
   ├─ Parse JSON response
   ├─ Display message in chat
   ├─ Update UI state
   └─ Text-to-speech (if enabled)

7. USER FEEDBACK
   └─ User sees/hears response ✓
```

---

This architecture provides:
- **Scalability** - Easy to add new components
- **Maintainability** - Clear separation of concerns
- **Testability** - Each module can be tested independently
- **Extensibility** - Simple to add new intents and handlers
- **Performance** - Lightweight frontend, efficient backend
