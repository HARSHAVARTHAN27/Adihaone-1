{
  "buildCommand": "pip install -r ai_assistant/requirements.txt",
  "outputDirectory": ".",
  "framework": "flask",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/ai_assistant/backend/app.py"
    }
  ]
}
